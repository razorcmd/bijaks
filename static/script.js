// Contoh JavaScript: Menampilkan alert saat halaman dimuat
document.addEventListener('DOMContentLoaded', (event) => {
    console.log('Halaman portofolio telah dimuat!');
    // alert('Selamat datang di portofolio saya!'); // Aktifkan jika ingin alert muncul
});

// Anda bisa tambahkan fungsi JS lain di sini, misalnya untuk animasi